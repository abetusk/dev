NOTE: You will need to change the MAC address in the `udev/rules.d` files, the SSID and password
in the `wpa_supplicant` files.

If desired, change the access point name and password in the `hostapd.conf` file.
